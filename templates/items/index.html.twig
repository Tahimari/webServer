{% extends 'base.html.twig' %}

{% block title %}Shop{% endblock %}

{% block body %}
    {% if items %}
            <hr>
                <p>Shoes</p>
            <hr>
            <div class="row">
            {% for item in items %}
                    <div class="col">
                        <a href="/items/{{item.id}}">
                            <img src={{ item.image_url }} width="280">
                            <p>{{ item.title }}</p>
                        </a>
                        <p>{{ item.price }} zł</p>
                    </div>
            {% endfor %}
            {% else %}
                <p>No articles to display</p>
    {% endif %}
             </div>
{% endblock %}

{% block javascripts %}
    <script src="/js/main.js"></script>
{% endblock %}